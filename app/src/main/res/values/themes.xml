<resources>
    <style name="Theme.PutusAsap" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Samakan warna background dengan splash Compose -->
        <item name="android:windowBackground">@color/splash_red</item>
    </style>
</resources>
